# =====================================================================
# Production environment. Me file eka .env kiyala copy karala
# deploy/ folder eka æthule thiyanna. GIT EKATA COMMIT KARANNA EPA.
# =====================================================================

# --- Public hostname -----------------------------------------------
# DuckDNS subdomain ekak ho ohuge domain ekak.
# Meka DNS eken VM eke public IP ekata point wenna one.
PUBLIC_HOST=clinic-api.duckdns.org

# Let's Encrypt eken certificate expiry warnings me email ekata enawa
ACME_EMAIL=you@example.com

# --- Database -------------------------------------------------------
POSTGRES_DB=clinicdb
POSTGRES_USER=clinic
# openssl rand -base64 24
POSTGRES_PASSWORD=REPLACE_ME

# --- RabbitMQ -------------------------------------------------------
RABBITMQ_USER=clinic
# openssl rand -base64 24
RABBITMQ_PASSWORD=REPLACE_ME

# --- JWT ------------------------------------------------------------
# openssl rand -base64 48
# MEKA DEV EKE EKA NEMEI. Wenama ekak generate karanna.
JWT_SECRET=REPLACE_ME

# --- CORS -----------------------------------------------------------
# Frontend ekak thiyenawa nam ekage origin eka. Naethnam hisswa
# thiyanna -- API eka Swagger eken withrai use karanne.
CORS_ORIGINS=
