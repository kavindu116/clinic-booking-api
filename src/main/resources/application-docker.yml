# Docker container ekak athule run wenakota
spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    lk.kavindu.clinic: INFO
    org.hibernate.SQL: WARN
app:
  seed:
    enabled: false
